# Azure AI Foundry Browser & Computer Use API
azure-ai-agents>=1.2.0b2
azure-ai-projects
azure-identity
httpx
openai
Pillow
python-dotenv

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Must match the browser version baked into the Dockerfile base image
playwright==1.57.0

# Azure Blob Storage for screenshot storage
azure-storage-blob>=12.0.0
