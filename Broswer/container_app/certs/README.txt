Place corporate root CA certificates here as .crt files (PEM encoded).
They are installed into the system trust store and Chromium's NSS store at build time.
This directory is intentionally empty by default; the build works with no certs present.
